# Database
DB_HOST=your-db-host
DB_PORT=your-db-port
DB_NAME=your-db-name

# Auth
JWT_SECRET=your-jwt-secret
APP_ENV=development
DEBUG=false

# This key exists in .env.example but not in .env
REDIS_URL=your-redis-url
SMTP_HOST=your-smtp-host